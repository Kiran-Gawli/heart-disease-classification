# heart-disease-classification
build a ML model to predict heart disease using health data. The project includes data preprocessing, feature encoding, scaling, SMOTE for class imbalance, and model training using KNN, Decision Tree, Random Forest, Logistic Regression, and SVM with GridSearchCV. Models evaluated using accuracy, precision, recall, F1-score, and confusion matrix.
